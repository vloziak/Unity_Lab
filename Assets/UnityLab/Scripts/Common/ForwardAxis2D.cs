namespace UnityLab.Components
{
    /// <summary>
    /// Which local axis counts as "forward" when aiming or rotating a 2D object.
    /// Right is the object's local X axis (transform.right). Up is the local Y axis (transform.up).
    /// </summary>
    public enum ForwardAxis2D
    {
        Right,
        Up
    }
}
